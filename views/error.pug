doctype html
html(lang='vi')
    head
        meta(charset='UTF-8')
        meta(name='viewport', content='width=device-width, initial-scale=1.0')
        link(rel='preconnect', href='https://fonts.googleapis.com')
        link(rel='preconnect', href='https://fonts.gstatic.com', crossorigin='')
        link(href='https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap', rel='stylesheet')
        link(rel='stylesheet', href='/css/home.css')
        link(rel='stylesheet', href='/css/footer.css')
        link(rel='shortcut icon', type='image/png', href='/img/favicon.png')
        title Bạn đang gặp rắc rối!
    body
        header.header
            nav.nav.nav--tours
                a.nav__el(href='/overview') Tất cả chuyến đi
                form.nav__search
                    button.nav__search-btn
                        svg
                            use(xlink:href='/img/icons.svg#icon-search')
                    input.nav__search-input(type='text', placeholder='Tìm kiếm')
            .header__logo
                img(src='/img/logo-white.png', alt='Natours logo')
            nav.nav.nav--user
                if user
                    a.nav__el.nav__el--logout Đăng xuất
                    a.nav__el(href='/account')
                        img.nav__user-img(src=`/img/users/${user.name}`, alt=`This is photo of ${user.name}`)
                        span=user.name.split(' ')[0]
                else 
                    a.nav__el(href='/login') Đăng nhập
                    a.nav__el.nav__el--cta(href='/signup') Đăng ký

        main.main
            .error
                .error__title
                    h2.heading-secondary.heading-secondary--error Có gì đó sai ở đây!  
                    h2.error__emoji 😢 🤯
                .error__msg=msg

        script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/1.6.8/axios.min.js')
        script(src='/script/login.js') 
        script(src='/script/logout.js') 